@model IEnumerable<Gig.PensionCalc.Domain.Work>

@{
    ViewBag.Title = "Works";
}

<h2>Works</h2>
@{ 
    ViewData.TemplateInfo.HtmlFieldPrefix = "Works[]";
}
<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PeriodStart)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PeriodEnd)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Position)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Money)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model)
{
    var i = Array.IndexOf(List, item)
    <tr>
        <td>
            @*////////Изменить Item на Model[i]*@

            @Html.EditorFor(modelItem => item.PeriodStart) 
        </td>
        <td>
            @Html.EditorFor(modelItem => item.PeriodEnd)
        </td>
        <td>
            @Html.EditorFor(modelItem => item.Position)
        </td>
        <td>
            @Html.EditorFor(modelItem => item.Money)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
